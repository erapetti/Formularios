<% var cols = []; -%>
Cédula	Nombre	Correo	Teléfono<%
config.modulos.forEach(function(m){
   if (m.nombre !== null && m.nombre !== "cedula" && m.nombre !== "nombre" && m.nombre !== "correo" && m.nombre !== "telefono") {
	cols.push(m.nombre); -%>
	"<%= m.etiqueta -%>"
<% }
}) %>
<% recibido.forEach(function(item) { -%>
"<%= item.cedula %>"	"<%= item.nombre %>"	"<%= item.correo %>"	"=""<%= item.telefono %>"""<%
  cols.forEach(function(c){ -%>	"<%- item.json[c] || item.json[c]==0 ? item.json[c] : '' %>"<%
  })
}) %>

