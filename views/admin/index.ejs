<div id="admin">

  <h1> <%= config.titulo %> </h1>

  <form method="post" id="adminForm">

    <div class="panel panel-default content">
      <div class="panel-body">
        <table class="table formularios">
          <tr>
            <th>Id</th>
            <th>Título</th>
            <th>Desde</th>
            <th>Hasta</th>
            <th>Recibidos</th>
            <th>Borrados</th>
            <th>Acciones</th>
          </tr>
          <% config.forEach(function(item){ %>
            <tr formid="<%= item.formid %>" publico="<%= item.publico ? "on" : "" %>">
              <td>
                <%= item.formid %>
              </td><td>
                <%= item.titulo %>
              </td><td date="<% if (item.desde) { %><%= item.desde.fecha_edit_toString() %><% } %>">
                  <% if (item.desde) { %><%= item.desde.fecha_toString() %><% } %>
                </td><td date="<% if (item.hasta) { %><%= item.hasta.fecha_edit_toString() %><% } %>">
                  <% if (item.hasta) { %><%= item.hasta.fecha_toString() %><% } %>
                </td><td>
                  <span class="badge">
                    <%= cantidad[item.formid]>0 ? cantidad[item.formid] : 0 %>
                  </span>
                </td><td>
                  <span class="badge red">
                    <%= borrados[item.formid]>0 ? borrados[item.formid] : 0 %>
                  </span>
                </td><td class="acciones">
                  <a href="download?formId=<%= item.formid %>"<% if (!(cantidad[item.formid]>0)) { %> class="disabled"<% } %> title="descargar"><span class="glyphicon glyphicon-download-alt" aria-hidden="true"></span></a>
                  <a href="#" preview="<%= item.formid %>" dest="../form/preview" data-toggle="modal" data-target="#preview" title="previsualizar"><span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span></a>
                  <a href="#" edit="<%= item.formid %>" data-toggle="modal" data-target="#myModal" title="editar"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
                  <a <% if ((cantidad[item.formid]>0)) { %>href="#" class="disabled"<% } else { %>href="../form/modedit?formId=<%= item.formid %>"<% } %> title="editar componentes"><span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span></a>
                  <a href="#" dest="clonar" data="<%= item.formid %>" title="duplicar"><span class="glyphicon glyphicon-duplicate" aria-hidden="true"></span></a>
                  <a href="#" dest="borrar" data="<%= item.formid %>"<% if (cantidad[item.formid]+borrados[item.formid]>0) { %> class="disabled"<% } %> title="borrar"><span class="glyphicon glyphicon-remove-sign" aria-hidden="true"></span></a>
                <td>
              </tr>
          <% }) %>
        </table>
      </div>
    </div>

  </form>

<%- include edit.ejs %>
<%- include preview.ejs %>

</div>
