<div id="admin">

  <h1> <%= config.titulo %> </h1>

  <form method="post" id="adminForm">

    <div class="panel panel-default content">
      <div class="panel-body">
        <table class="table formularios">
          <tr>
            <th>Id</th>
            <th>Título</th>
            <th>Desde</th>
            <th>Hasta</th>
            <th style="text-align:center"><span class="glyphicon glyphicon-inbox" aria-hidden="true"></span></th>
            <th style="text-align:center"><span class="glyphicon glyphicon-cog" aria-hidden="true"></span></th>
          </tr>
          <% config.forEach(function(item){ %>
            <tr>
              <td>
                <%= item.formid %>
              </td><td>
                <%= item.titulo %>
              </td><td>
                <%= item.desde.toString() %>
                </td><td>
                  <%= item.hasta.toString() %>
                </td><td style="text-align:center">
                  <span class="badge">
                    <a href="#"><%= cantidad[item.formid]>0 ? cantidad[item.formid] : 0 %></a>
                  </span>
                </td>
                <td style="text-align:center">
                  <a href="#"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
                  &nbsp;
                  <a href="/admin/download?id=<%= item.formid %>"><span class="glyphicon glyphicon-download-alt" aria-hidden="true"></span></a>
                <td>
              </tr>
          <% }) %>
        </table>
      </div>
    </div>

  </form>

</div>
